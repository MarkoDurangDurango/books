{
  "name": "bookshelf-resolver",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
